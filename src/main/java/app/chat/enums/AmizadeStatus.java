package app.chat.enums;

public enum  AmizadeStatus {
    PENDING,     // convite enviado
    ACCEPTED,    // amizade confirmada
    REJECTED,    // rejeitado (opcional)
    BLOCKED      // bloqueado (opcional)
}
